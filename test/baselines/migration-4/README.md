This directory stores machine-readable baseline artifacts for Milestone 4.

Current artifacts:

- `runtime-core-baseline.json`: repeated benchmark summary for run-finalization, pricing-batch, and stash-refresh fixture workloads
