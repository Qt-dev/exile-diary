This directory stores machine-readable baseline artifacts for Milestone 6.

Current artifacts:

- `ocr-app-path-baseline.json`: repeated OCR benchmark summary for the in-process control path and the OCRWatcher sidecar path
