# Migration 0 Baselines

This directory stores machine-readable baseline artifacts for Milestone 0.

Expected contents:

- `core-baseline.json`: repeated deterministic benchmark summary
- additional app lifecycle outputs when `--include-app` is used

These files are intended to be diffed between the current implementation and later refactor branches.
