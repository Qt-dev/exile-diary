# Packet 02: renderer

## Objective

Consume 3.29 modifier objects safely in the tooltip and renderer item domain.

## Ownership

Worker owns `src/renderer/components/Item/ItemTooltip.tsx` and `src/renderer/stores/domain/item.ts`.
