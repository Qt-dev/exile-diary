# Packet 03: tests

## Objective

Add focused tests and fixtures for current and legacy PoE item payloads.

## Ownership

Worker owns relevant files under `test/main`, `test/renderer`, and `test/ui/fixtures.ts`.
